# Efectos en jQuery

- ¿Para que sirven?
   - `.fadeIn()`
   - `.fadeOut()`
   - `.fadeToggle()`
   - `.hide()`
   - `.show()`
   - `.toggle()`
   - `.slideDown()`
   - `.slideUp()`
   - `.slideToggle()`